<!DOCTYPE html>
<html>
  <head>
    <title>The Bathroom Wall</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width">
    <link href="https://fonts.googleapis.com/css?family=Nothing+You+Could+Do" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Lato|Loved+by+the+King|Nothing+You+Could+Do" rel="stylesheet">
    

  </head>

  <body>

    <!-- Navigation Bar -->
    <center><nav class="navbar fixed-bottom navbar-dark bg-faded navbar-toggler">
      <div class="container">
        <!-- Icons -->
        <div class="icons-nav">
        <% if user_signed_in? %>
          <%= link_to(image_tag("home.png"), posts_path) %>
          <%= link_to(image_tag("new-post.png"), new_post_path) %>
          <%= link_to(image_tag("following.png"), feed_path) %>
            <%= link_to user_path(current_user.username) do %>
            <%= image_tag ("me.png") %>
          <% end %>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% flash.each do |name, message| %>
      <%= content_tag(:div, message, class: "alert alert-info #{name}") %>
    <% end %>

    <!-- Page Content -->
    <%= yield %>
    
    <!-- Footer -->
    <footer class="small text-muted">
      <div class="container">
      
        <hr>
        <center>
        
        </center>
      </div>
    </footer>
  </body>
</html>
